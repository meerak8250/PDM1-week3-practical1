let x=300
let y=300
let fillColor= color(random(255),random(255), random(255));
let shapeSelect=true

function setup(){
createCanvas(600,600);
fillColor= color(random(255),random(255), random(255));
}

function draw(){
    background(0);
    fill(255);
    rect(x,y,300,200);

    function mousePressed(){
        if(mouseX<300)
    }










}